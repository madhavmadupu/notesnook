# Copy this file to apps/web/.env.local and fill in from
# servers/convex/.env.local (the value of CONVEX_URL, NOT CONVEX_DEPLOYMENT).
#
# Only variables prefixed with NN_ are exposed to the client bundle
# (see apps/web/vite.config.ts:envPrefix).
NN_CONVEX_URL=https://your-deployment.convex.cloud
